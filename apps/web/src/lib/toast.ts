export function showError(message: string) {
  // This function can be used to show error messages in the UI.
  // You can implement it using your preferred UI library or framework.
  console.error(message);
  // For example, if you're using a toast library:
  // toast.error(message);
}

export function showSuccess(message: string) {
  // This function can be used to show success messages in the UI.
  // You can implement it using your preferred UI library or framework.
  console.log(message);
  // For example, if you're using a toast library:
  // toast.success(message);
}

export function showInfo(message: string) {
  // This function can be used to show informational messages in the UI.
  // You can implement it using your preferred UI library or framework.
  console.info(message);
  // For example, if you're using a toast library:
  // toast.info(message);
}
